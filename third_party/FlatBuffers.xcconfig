// FlatBuffers Build Configuration
// Header-only C++ library for serialization
// Version: v25.12.19 (git submodule)

// Header search paths - FlatBuffers is header-only
// Projects are in iMuJoCo/{app,core,render}/, so ../../third_party/ goes to root third_party/
FLATBUFFERS_INCLUDE = $(PROJECT_DIR)/../../third_party/flatbuffers/include

// Add to header search paths (same for all platforms - header-only)
HEADER_SEARCH_PATHS = $(inherited) $(FLATBUFFERS_INCLUDE)
